{
  "name": "unicorn-studio-gallery",
  "version": "0.0.0",
  "private": true,
  "description": "Unicorn Studio internal gallery platform — capture CLI, viewer, and review platform.",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:capture": "pnpm --filter @unicorn-studio/gallery-capture dev"
  }
}
